# Test Strategy Template

## Unit Tests
Domain transitions, validation, deployment rules, rollback rules and metric calculations.

## API Tests
Success, invalid requests, missing records, conflicts, duplicates and authorization where used.

## Integration Tests
Persistence, deployment, retry, rollback and metrics.

## Angular Tests
Components, services, loading, errors, validation and API interaction.

## End-to-End Scenario
Register model → register version → approve → deploy → view metrics → roll back.

## CI, Coverage and Limitations
